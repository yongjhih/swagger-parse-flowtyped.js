/* @flow */

import axios from 'axios';
import {Axios} from 'axios';

{{> types }}

default export class {{&className}} {
  _axios: Axios => *;

  constructor(baseURL: string = "{{&domain}}") {
    this._axios = axios.create({
      baseURL: baseURL
    })
  }

  {{> methods }}
}
